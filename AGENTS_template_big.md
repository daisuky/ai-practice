# AGENTS.md｜{プロジェクト名}

> このファイルは、OpenAI Codexなどのコーディングエージェントに対する、
> このリポジトリの作業指示書です。
>
> `{}` で囲まれた箇所を、このプロジェクトの内容に合わせて記入してください。
> 不要なセクションは削除して構いません。

## プロジェクト概要

- **目的**: {このプロジェクトが解決しようとしている課題}
- **対象ユーザー**: {誰が使うものか}
- **使用言語**: {例: TypeScript、Python、Go}
- **フレームワーク・主要ライブラリ**: {例: Next.js、React、FastAPI}
- **実行環境**: {例: Windows、Linux、Docker、Node.jsのバージョン}
- **パッケージマネージャー**: {例: npm、pnpm、yarn、uv、Poetry}

## リポジトリ構成

```text
{プロジェクトの主要なディレクトリ構成を記載}
```

主要ディレクトリの役割:

- `{ディレクトリ}`: {役割}
- `{ディレクトリ}`: {役割}
- `{ディレクトリ}`: {役割}

## Codexへの基本指示

1. 作業前に、関連するファイル、既存の実装、設定ファイル、テストを確認してください。
2. 変更前に、変更の目的、対象ファイル、影響範囲を簡潔に説明してください。
3. 依頼の範囲を超える変更や、大規模なリファクタリングは避けてください。
4. 既存の設計、命名規則、コーディングスタイル、ディレクトリ構成を尊重してください。
5. 不明な仕様や重要な判断がある場合は、勝手に決めずに質問してください。
6. コードを変更した場合は、可能な範囲で適切なテスト・静的チェック・ビルドを実行してください。
7. 実行したコマンドと、その結果を最終報告に記載してください。
8. 実行できなかった検証がある場合は、理由を明記してください。
9. 最終報告では、変更内容、検証内容、未解決事項を簡潔にまとめてください。

## 開発・検証コマンド

以下のコマンドを使用してください。プロジェクトに存在しないものは削除してください。

```bash
# 依存関係のインストール
{例: npm install}

# 開発サーバーの起動
{例: npm run dev}

# テスト
{例: npm test}

# リント
{例: npm run lint}

# 型チェック
{例: npm run typecheck}

# ビルド
{例: npm run build}
```

## コーディングスタイル

- **インデント**: {スペース2つ / スペース4つ / タブ}
- **命名規則**: {例: 変数はcamelCase、クラスはPascalCase}
- **文字コード**: {例: UTF-8}
- **コメント・ドキュメントの言語**: {日本語 / 英語 / その他}
- **関数・クラスの設計方針**: {例: 小さく単一責任にする}
- **その他**: {プロジェクト固有のルール}

## テスト方針

- **テストフレームワーク**: {例: Vitest、Jest、pytest}
- **テストファイルの配置**: {例: `tests/`、対象ファイルの隣}
- **必ず確認するケース**: {正常系、異常系、境界値など}
- **変更時に必要な検証**: {実行すべきテストやチェック}

## 禁止事項・安全上の注意

- APIキー、パスワード、トークンなどの秘密情報をコードやコミットへ含めないでください。
- 本番環境、実在の外部サービス、重要なデータベースへ、許可なく接続・変更しないでください。
- `{変更禁止ファイルまたはディレクトリ}` を変更しないでください。
- `{実行禁止の操作}` を実行しないでください。
- 個人情報や機密情報を、ログ・テストデータ・サンプルへ含めないでください。

## 変更・削除に関するルール

- **削除してはいけないファイル**: `{ファイルまたはディレクトリ}`
- **変更前に確認が必要な操作**: `{例: ファイル削除、本番設定変更、DBスキーマ変更}`
- **データ移行が必要な場合の方針**: `{方針}`

## プロジェクト固有の用語

| 用語・略語 | 意味 |
|---|---|
| `{用語}` | `{意味}` |
| `{用語}` | `{意味}` |

## 参考にするべきファイル

- `{ファイルパス}`: {このファイルを参照する理由}
- `{ファイルパス}`: {このファイルを参照する理由}
- `{ドキュメントのパス}`: {仕様・設計・API情報など}

## 作業完了時の報告形式

以下の形式で報告してください。

### 変更内容

- {変更点}

### 変更ファイル

- `{ファイルパス}`: {変更内容}

### 検証

- `{実行したコマンド}`: {結果}

### 補足

- {未確認事項、制約、利用者が手動で行う作業など}

## サブディレクトリ固有の指示

特定のディレクトリだけに適用するルールがある場合は、そのディレクトリ内に別の `AGENTS.md` を配置してください。
例:

```text
AGENTS.md
src/AGENTS.md
tests/AGENTS.md
```