# AGENTS.md｜{ai-practice}

> このファイルは、OpenAI Codexなどのコーディングエージェントに対する、
> このリポジトリの作業指示書です。
>
> `{}` で囲まれた箇所を、このプロジェクトの内容に合わせて記入してください。
> 不要なセクションは削除して構いません。

## プロジェクト概要

- **目的**: AI仙人のドリルをこなすプロジェクトでよーだの学習を支援する
- **使用言語**: Python、JavaScript、HTML
- **主要ツール**: Node.js、ChatGPT、ChatGPT Codex、Ollama（LLM 2個インストール済み）

JavaScriptの実行にはNode.jsを使用してください。利用可能なコマンドは以下のとおりです。

- **インストール**: `npm install`
- **テスト**: `npm test`
- **リント**: `npm run lint`
- **開発用実行**: `npm run dev`

コマンドが存在しない場合は、新しいコマンドを推測せず、`package.json`を確認してからユーザーに質問してください。

実際のプロジェクトで使用しているコマンドに合わせて記載すると、より有効です。

- **リポジトリ構成**: ルートは `I:\ai-practice`
  - ドリルごとにフォルダを作るなど、前の学習テーマを無理に混同させないでください。

## Codexへの基本指示

1. コードを変更する前に、必ず変更の目的と影響範囲を説明してください。
2. 既存のファイルを削除する場合は、事前に確認を取ってください。
3. テストを自動で実行しないでください。実行前に必ず確認を求めてください。
4. コメントは日本語で書いてください。

次の操作は実行前に確認してください。

- ファイルやフォルダの削除・移動・名前変更
- 依存パッケージの追加・更新
- 環境変数、認証情報、設定ファイルの変更
- 複数のドリルにまたがる大規模な変更
- Gitのコミット、プッシュ、ブランチ削除


## 禁止事項

- **絶対にやらないこと**: API課金を使うような処理を大きく始めること
- **変更禁止ファイル**: `AGENTS_template_big`、`AGENTS_template_compact.md`

## コーディングスタイル

- **インデント**: 4つ
- **命名規則**: `snake_case`
- **関数の長さ**: 50行以内を目安とする

## よく使う用語・略語の定義

| 用語 | 意味 |
|------|------|
| MBA | Master Business Administration |


## 参考にするべきファイル

- `docs/architecture.md`: システム全体の設計図
- `docs/api_spec.md`: APIの仕様書